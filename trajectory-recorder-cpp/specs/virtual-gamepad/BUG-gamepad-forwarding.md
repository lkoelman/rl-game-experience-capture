I'm running the virtual gamepad bridge example application together with my video game. The virtual gamepad is not being picked up by my game. My current workflow is: launch gamepad bridge -> launch video game (full screen) -> video game gets window focus. One hypothesis is that the physical gamepad inputs might not be picked up by the gamepad bridge application after it loses focus, or that the bug is due to another window focus related issue. Help me diagnose the problem and fix the bug. Think of different possible causes, rank them by likelihood, and formulate a diagnosis and debug strategy.

Start by modifying the gamepad bridge so that it prints the forwarded actions in the format "<physical_gamepad_id>.<button> -> <virtual_gamepad_id>.<button>", rate limited to 30 printed lines per second (max rate can be changed with command line argument)