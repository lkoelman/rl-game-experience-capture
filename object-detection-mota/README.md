# Tomota

Playground for object detection and multi-object tracking

## Installation

```sh
# Create venv with packages and all dependencies
uv sync --all-extras

# install pre-commit hooks
uv run prek install
```